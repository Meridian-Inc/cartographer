name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: backend/requirements.txt
      - run: pip install -r requirements.txt
      - run: pytest tests/ -q --tb=no --cov=app --cov-report=xml --cov-fail-under=90
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: backend/coverage.xml
          flags: backend
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run test:coverage -- --reporter=dot
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: frontend/coverage/coverage-final.json
          flags: frontend
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  assistant-service:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: assistant-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: assistant-service/requirements.txt
      - run: pip install -r requirements.txt
      - run: pytest tests/ -q --tb=no --cov=app --cov-report=xml --cov-fail-under=95
        env:
          OPENAI_API_KEY: ""
          ANTHROPIC_API_KEY: ""
          GOOGLE_API_KEY: ""
          GEMINI_API_KEY: ""
          METRICS_SERVICE_URL: "http://localhost:8003"
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: assistant-service/coverage.xml
          flags: assistant-service
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  auth-service:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: auth-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: auth-service/requirements.txt
      - run: pip install -r requirements.txt
      - run: pytest tests/ -q --tb=no --cov=app --cov-report=xml --cov-fail-under=95
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: auth-service/coverage.xml
          flags: auth-service
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  health-service:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: health-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: health-service/requirements.txt
      - run: pip install -r requirements.txt
      - run: pytest tests/ -q --tb=no --cov=app --cov-report=xml --cov-fail-under=95
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: health-service/coverage.xml
          flags: health-service
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  metrics-service:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: metrics-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: metrics-service/requirements.txt
      - run: pip install -r requirements.txt
      - run: pytest tests/ -q --tb=no --cov=app --cov-report=xml --cov-fail-under=95
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: metrics-service/coverage.xml
          flags: metrics-service
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  notification-service:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: notification-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: notification-service/requirements.txt
      - run: pip install -r requirements.txt
      - run: pytest tests/ -q --tb=no --cov=app --cov-report=xml --cov-fail-under=80
      - uses: codecov/codecov-action@v4
        if: always()
        with:
          file: notification-service/coverage.xml
          flags: notification-service
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

